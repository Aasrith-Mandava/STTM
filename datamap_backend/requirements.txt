google-adk==1.18.0
google-genai
pandas
numpy
python-dotenv
openpyxl
chardet
ydata-profiling
db-dtypes
uvicorn
fastapi==0.121.1
httpx
pydantic
# AI Launchpad SSO: validate the Launchpad's HS256 JWT (APP_SESSION_AUTH_MODE=launchpad_sso)
PyJWT>=2.8.0
streamlit
reportlab==4.4.5
starlette==0.49.3
tabulate==0.9.0
pypdf==5.3.0
python-docx
docx2pdf
docx2txt  # Alternative DOCX text extraction
# Standalone (no GCP/Vertex): local DB + vector store
sqlalchemy>=2.0.0
sqlite-vec
# Optional Postgres app DB driver (app DB defaults to local SQLite)
psycopg2-binary>=2.9.9
# Optional Indemap DB Integration (SQL Server) — install manually if needed:
#   pyodbc>=5.0.0
#   pymssql>=2.2.8
# Multi-provider LLM (Groq + Gemini via LiteLLM)
litellm
